<h1>New post</h1>

<div>
	<%= form_for @post do |f|%>
		<%= render 'errors' %>
		<%= f.label :title %>
		<%= f.text_field :title%>

		<%= f.label :content %>
		<%= f.text_area :content %>

		<%= f.submit "Create"%>
	<%end%>
</div>